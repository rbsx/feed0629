import useLatestTweets from './useLatestTweets'

let pollInterval = 2000

describe('useLatestTweets()', () => {
  test('updateTweets should be dispatched on mount', () => {})

  test('updateTweets should be dispatched every 2 seconds', () => {})

  test('updateTweets should be not be dispatched every 2 seconds if paused', () => {})
})
